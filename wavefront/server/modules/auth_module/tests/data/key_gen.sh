# run the below code in terminal on appropriate folder to generate pvt and pub key

# $ssh-keygen -t rsa -P "" -b 4096 -m PEM -f test_private_key.pem
# $ssh-keygen -e -m PEM -f test_private_key.pem > test_public_key.pem
