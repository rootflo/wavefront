[project]
name = "llm-inference-config-module"
version = "0.1.0"
description = "LLM Inference Configuration module for dynamic model and inference engine configuration"
authors = [
    { name = "rootflo engineering", email = "engineering@rootflo.ai" }
]
requires-python = ">=3.11"

dependencies = [
    "common-module",
    "db-repo-module",
    "fastapi>=0.100.0",
    "pydantic>=2.0.0",
    "dependency-injector>=4.40.0",
    "httpx>=0.25.0",
]

[tool.uv.sources]
common-module = { workspace = true }
db-repo-module = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=9.0.3,<10.0.0",
    "pytest-asyncio>=1.0.0,<2.0.0",
    "asyncpg>=0.30.0,<1.0.0",
]

[tool.uv]
package = true
# Inert here: uv reads resolver settings only from the workspace root
# (wavefront/server/pyproject.toml), which is where the real cooldown lives.
# Kept so the setting survives if this package is ever split out.
exclude-newer = "7 days"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["llm_inference_config_module"]
