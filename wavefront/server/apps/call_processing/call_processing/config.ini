[env_config]
app_env = ${APP_ENV}
app_name_floware = ${APP_NAME_FLOWARE:floware}
app_name = ${APP_NAME:call_processing}
floware_base_url = ${FLOWARE_BASE_URL:http://localhost:8001}
passthrough_secret = ${PASSTHROUGH_SECRET:}
call_processing_base_url = ${CALL_PROCESSING_BASE_URL:http://localhost:8003}

[web]
allowed_origins=${ALLOWED_ORIGINS:http://localhost:8001}

[cloud]
provider = ${CLOUD_PROVIDER:}
region = ${CLOUD_REGION:}
project_id = ${CLOUD_PROJECT_ID:}
location = ${CLOUD_LOCATION:}
